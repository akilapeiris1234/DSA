
// Image paths and routes are config, not logic — keep them separate from components

export const AUTH_IMAGES = {
    background: '/images/login.png',
    homeBackground: '/images/home.png',
} as const;
